import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  image: {
    borderRadius: 16,
    width: 32,
    height: 32,
  },

  name: {
    marginLeft: 16,
    lineHeight: 32,
    fontSize: 16,
  },
});
